# =============================================================================
# 开发与测试依赖(不进入生产镜像)
#
# 安装: pip install -r requirements-dev.txt
# =============================================================================

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0                       # TestClient 依赖

# 代码质量
ruff>=0.6.0                         # lint + format(替代 flake8/isort/black)
mypy>=1.10.0                        # 静态类型检查

# 评测(阶段 P5 使用)
# ragas>=0.1.0                      # RAG 专用评测框架, 如需可启用
