# =============================================================================
# DocMind 运行依赖
#
# 安装:      pip install -r requirements.txt
# 国内加速:  pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 说明: 已知本机已装的包使用精确版本锁定, 其余使用下限约束;
#       跑通后会执行 `pip freeze > requirements.lock.txt` 生成可复现的锁文件.
# =============================================================================

# ---------------------------- Web 框架 -------------------------------------
fastapi==0.136.3
uvicorn[standard]>=0.49.0
python-multipart>=0.0.18            # UploadFile 文件上传必需
sse-starlette>=2.1.3                # SSE 流式输出(问答打字机效果)

# ---------------------------- 配置与校验 -----------------------------------
pydantic==2.12.5
pydantic-settings==2.14.1

# ---------------------------- PDF 解析 -------------------------------------
pymupdf>=1.24.0                     # 主力解析器: 可取文本块坐标, 支持双栏还原
pypdf>=5.0.0                        # 兜底解析器: 与 PyMuPDF 交叉验证, 防单点失效

# ---------------------------- 分块与关键词检索 -----------------------------
jieba>=0.42.1                       # 中文分词, BM25 的必要前提
rank-bm25>=0.2.2                    # 关键词检索, 补齐向量对型号/数字的短板

# ---------------------------- 向量化与重排 ---------------------------------
sentence-transformers>=3.0.0        # 本地 Embedding + Reranker
transformers>=4.40.0
torch>=2.2.0                        # 有 CUDA 时 sentence-transformers 自动使用 GPU

# ---------------------------- 向量数据库 -----------------------------------
chromadb>=1.0.0                     # 默认 embedded 模式, 零外部依赖

# ---------------------------- 大模型 ---------------------------------------
openai>=1.50.0                      # OpenAI 兼容协议, 可对接 DeepSeek / 通义 / 硅基流动
httpx>=0.27.0
tenacity>=9.0.0                     # LLM 调用重试与退避

# ---------------------------- 存储与异步任务 -------------------------------
sqlalchemy>=2.0.30
aiosqlite>=0.20.0                   # 开发环境 SQLite 异步驱动(生产可换 asyncpg/aiomysql)
aiofiles>=23.2.1                    # 异步落盘大文件
redis>=5.0.0                        # 任务队列 / 缓存
rq>=1.16.0                          # 轻量任务队列(比 Celery 更贴合本项目规模)

# ---------------------------- 语音交互(可选) -------------------------------
# 不装也能跑: 语音是增强功能, 缺依赖时接口返回 503 并提示安装命令,
# 不会影响问答与面试的文字链路. 这也是为什么这两个包是**延迟导入**的.
edge-tts>=7.0.0                     # TTS: 微软在线神经音色, 免费且无需 Key
dashscope>=1.20.0                   # ASR: 阿里云 Paraformer(同时是 LLM 的服务商选项之一)
